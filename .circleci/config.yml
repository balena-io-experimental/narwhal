version: 2

machine:
  services:
    - docker

jobs:
  build:
    docker:
      - image: circleci/rust:1
    steps:
      - setup-docker-engine
      - checkout
      - run: cargo test -- --nocapture

